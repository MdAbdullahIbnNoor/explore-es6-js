const a = 10;
const b = 20;

const math = `THe sum of ${a} and ${b} is ${a + b}`;
console.log(math);

const email = `Hi john
What is about you?
This is a new ES6 feature`;

console.log(email);

const number = [45, 48, 75, 78];
const student = {
    name: 'John Cena',
    age : 5,
}

const message = `The sum of ${number[0]} and ${student.age} is ${number[0] + student.age}`;
console.log(message);